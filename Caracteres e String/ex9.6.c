#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

int main(){
    char str[10];
    int n;

    printf("Nome: ");
    scanf("%s", str);

    for(i = 0; i < n; i++){
        printf("%s\n", str);
    }
}