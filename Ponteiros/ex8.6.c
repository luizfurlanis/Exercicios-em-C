#include <stdio.h>
#include <stdlib.h>

void pInterno(const double *a1, const double *a2, double *pi, int n);
int main(){
    return 0;
}

void pInterno(const double *a1, const double *a2, double *pi, int n){
    
}